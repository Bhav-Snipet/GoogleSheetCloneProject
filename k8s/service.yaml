apiVersion: v1
kind: Service
metadata:
  name: googlesheetclone-service
  namespace: "2401025"
  labels:
    app: googlesheetclone
spec:
  type: ClusterIP
  selector:
    app: googlesheetclone
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
